machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t matisq/jenkins-slave .

test:
  override:
    - docker run -i -p 222:22 matisq/jenkins-slave java -version
